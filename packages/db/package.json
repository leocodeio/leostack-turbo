{
  "name": "@leostack/db",
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "db:gen": "pnpm prisma:local",
    "prisma:local": "npx prisma generate && npx @better-auth/cli generate --config apps/web/app/server/services/auth/db.server.ts -y && npx prisma migrate dev",
    "prisma": "npx prisma generate && npx prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
